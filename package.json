{
  "name": "focuslens-server",
  "version": "1.0.0",
  "description": "FocusLens backend — activity ingest, AI categorisation, reports",
  "type": "module",
  "main": "server/index.js",
  "scripts": {
    "dev":   "nodemon server/index.js",
    "start": "node server/index.js",
    "db:init": "node -e \"import('./server/models/db.js').then(m => m.initDb())\""
  },
  "dependencies": {
    "cors":    "^2.8.5",
    "dotenv":  "^16.4.5",
    "express": "^4.19.2",
    "pg":      "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}